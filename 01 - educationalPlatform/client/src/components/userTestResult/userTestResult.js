import React from 'react';
import {observer} from "mobx-react-lite";

const UserTestResult = observer(() => {
    return (
        <div>
            UserTestResult
        </div>
    );
});

export default UserTestResult;